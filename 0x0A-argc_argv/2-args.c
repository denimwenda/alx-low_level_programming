#include <stdio.h>
/**
 * main - prints all arguments it receives
 * @argv: int
 * @argc: argument
 * Return: 0
 */
int main(int argc, char **argv)
{
	int b;

	for (b = 0; b < argc; b++;)
		printf("%s\n", argv[a]);

	return (0);
}
