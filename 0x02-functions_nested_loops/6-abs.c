#include "main.h"
/**
 * _abs - calculate absolute value of the number from zero
 * @n: parameter
 *
 * Return: absolute value of input
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
